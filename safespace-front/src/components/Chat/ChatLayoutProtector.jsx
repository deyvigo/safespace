export const ChatLayoutProtector = ({ children }) => {
  return (
    <div className="bg-blue-300 flex-1 flex flex-col overflow-hidden text-black">
      Big icon here when there are no selected conversation
    </div>
  )
}